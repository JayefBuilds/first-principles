# Playground instructions

This is a local Python training project.
Run tests with `python3 -m unittest discover -s tests -v` from this folder.
Use only Python standard-library modules.
When describing coverage, cite test names from `tests/test_lobby.py`.
Validate this file with `python3 exercises/03/check_contract.py CLAUDE.md`.
